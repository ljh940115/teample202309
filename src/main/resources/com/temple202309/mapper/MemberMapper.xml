<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.temple202309.mapper.MemberMapper">

	<select id="getMember" parameterType="String" resultType="com.temple202309.domain.MemberVO">
	SELECT userid, userpw, username
	       ,userphone, regdate, updatedate
	FROM member
	WHERE userid=#{userid}
	</select>
	<insert id="insertMember" parameterType="com.temple202309.domain.MemberVO" >
	INSERT INTO member (userid , userpw, username
		, userphone) VALUES(
		#{userid}, #{userpw}, #{username},
		#{userphone})
	</insert>
	<update id="updateUser" parameterType="com.temple202309.domain.MemberVO" >
	UPDATE member
	SET userphone = #{userphone}
	WHERE uesrid = #{uesrid}
	</update>
	
	<delete id="deleteUser" >
	 DELETE FROM member WHERE userid = #{userid}
	</delete>

</mapper>